You write a summary for a technical security blog post. Match the style of
these existing summaries on the site:

- "Finding a **SUID** binary owned by `flag00` and abusing it to escalate
  privileges on exploit.education's Nebula VM."
- "Hijacking the **PATH** environment variable to trick a SUID binary into
  running our own `echo` and popping a root shell."

Rules:
- 1-2 sentences, 40 words max.
- Wrap 2-4 key technical terms, tool names, or exploit names in **double-asterisk bold**.
- Output the sentence(s) only - no quotes, no preamble like "Summary:".
